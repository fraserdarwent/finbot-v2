{
  "name": "finbot-v2",
  "version": "1.0.0",
  "main": "src/main.js",
  "license": "Unlicensed",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "start:rollup": "rollup -c -w",
    "start:node": "while [ ! -f dist/bundle.js ];do echo '⌛ Waiting for bundle to be available';sleep 0.5;done;nodemon dist/bundle.js",
    "start": "npm-run-all --serial clean --parallel start:*"
  },
  "dependencies": {
    "@fraserdarwent/javascript-logger": "^1.0.4",
    "@rollup/plugin-commonjs": "^15.0.0",
    "@tensorflow/tfjs-node": "^2.3.0",
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.4",
    "npm-run-all": "^4.1.5",
    "promise-timeout": "^1.3.0"
  }
}
